# Names used in non-standard evaluation contexts, declared here so that
# `devtools::check()` doesn't complain about that.
utils::globalVariables(
  c(
    ".", "time", "hour", "minute", "wattage_type", "wattage", "StateOfCharge"
  )
)
